# 天气数据源配置（复制为 .env.local 后填写；.env.local 不会进版本库）
#
# 默认（不填任何东西）：使用 Open-Meteo，免费、无需 key、全球覆盖。
# 想换成国内「和风天气」：到 https://dev.qweather.com 注册，创建项目拿到 key，
# 填入下面的 VITE_QWEATHER_KEY 即可自动切换（无需改代码）。

# 和风天气 API Key（填了就自动切到和风）
VITE_QWEATHER_KEY=

# 和风天气 API Host。新账号在控制台「设置」里有专属 Host，形如
# https://abcd1234.qweatherapi.com —— 把下面两个都设为它。
# 老的免费账号可留默认值。
VITE_QWEATHER_HOST=https://devapi.qweather.com
VITE_QWEATHER_GEO_HOST=https://geoapi.qweather.com

# 可选：强制指定数据源，'qweather' 或 'openmeteo'（一般留空，按 key 自动判断）
VITE_WEATHER_PROVIDER=
