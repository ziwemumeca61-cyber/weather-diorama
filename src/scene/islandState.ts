// Shared vertical bob offset of the floating island, in world units. The whole
// diorama gently floats by translating on Y; People adds this to the hero's
// reported world position so the follow camera keeps tracking correctly.
export const islandState = { y: 0 }
